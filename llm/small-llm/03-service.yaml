apiVersion: v1
kind: Service
metadata:
  name: small-llm
  namespace: small-llm
  labels:
    app.kubernetes.io/name: small-llm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: small-llm
  ports:
    - name: http
      port: 11434
      targetPort: http
      protocol: TCP
