apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: gpu-role
  namespace: gpu-users
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log"]
    verbs: ["get", "list", "watch", "create", "delete"]
