apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: cpu-role
  namespace: cpu-users
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log"]
    verbs: ["get", "list", "watch"]

