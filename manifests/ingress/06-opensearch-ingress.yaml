apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opensearch-dashboards
  namespace: opensearch
spec:
  ingressClassName: nginx
  rules:
    - host: opensearch.44.207.222.188.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: opensearch-dashboard-opensearch-dashboards
                port:
                  number: 5601
