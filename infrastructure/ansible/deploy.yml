---
# ---------------------------------------------------------
# Master GPU Cluster Deployment (Laptop Runner)
# ---------------------------------------------------------

# ---------------------------------------------------------
# System validation
# ---------------------------------------------------------
- import_playbook: 1_validate_system.yml
  tags: [validate, system]

# ---------------------------------------------------------
# Base OS + utilities
# ---------------------------------------------------------
- import_playbook: 2_install_base_packages.yml
  tags: [packages, base]

# ---------------------------------------------------------
# GPU drivers
# ---------------------------------------------------------
- import_playbook: 3_install_drivers.yml
  tags: [drivers, gpu]

# ---------------------------------------------------------
#JupyterHub installation
# ---------------------------------------------------------
- import_playbook: 4_install_jupyterhub.yml
  tags: [jupyterhub, notebook]


## ---------------------------------------------------------
## SSH configuration (password login etc.)
## ---------------------------------------------------------
#- import_playbook: 4_password_login.yml
#  tags: [ssh, auth]
#
## ---------------------------------------------------------
## User + group setup
## ---------------------------------------------------------
#- import_playbook: 5_setup_users.yml
#  tags: [users, accounts]
#
## ---------------------------------------------------------
## Spack installation
## ---------------------------------------------------------
#- import_playbook: 6_install_spack.yml
#  tags: [spack, hpc]
#
## ---------------------------------------------------------
## CUDA toolkit
## ---------------------------------------------------------
#- import_playbook: 7_install_cuda.yml
#  tags: [cuda, gpu]
#
## ---------------------------------------------------------
## Intel MKL
## ---------------------------------------------------------
#- import_playbook: 8_install_mkl.yml
#  tags: [mkl, math]